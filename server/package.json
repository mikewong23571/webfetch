{
  "name": "server",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250731.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "2.21.3"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "test": "vitest",
    "build:schemas": "ajv compile -s src/schema.json -o src/compiled-schema.js -c ajv-formats"
  }
}
